main: ${PACKAGE}.${NAME}
name: ${NAME}
version: "${VERSION}"
api-version: "26.1.2"
author: AUTHOR
description: Tracks per-player PvP kill and death statistics with PlaceholderAPI support.
soft-depend:
  - PlaceholderAPI
